receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "localhost:11111"

exporters:
  otlp:
    endpoint: "locahost:14250"

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp]

